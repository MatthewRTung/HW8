package cs3500.planner.provider.view;

/**
 * Represents a panel for an event.
 */
public interface IEventPanel {
  // This is empty since all functionality can be taken from the schedule frame and panel.
  // In order to simplify our design, the controller only needs access to the schedule
  // frame in order to mutate the model.
}
